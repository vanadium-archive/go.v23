package base

// This file contains experimental features, that are not yet supported for all
// generated languages.  Run the following command to generate Go:
//
//   vdl -experimental -exts=.vdl,.vdlexp generate -lang=go v.io/core/veyron2/vdl/...
//
// TODO(toddw): Merge with base.vdl, and remove this file.

type (
	NamedEnum       enum{A;B;C}
	NamedUnion      union{A bool;B string;C int32}
)

type ScalarsExp struct {
	B13 NamedEnum
	B14 NamedUnion
}

const (
	Cenum  = NamedEnum.A
	Cunion = NamedUnion{A: true}
	Carray = [3]int32{1, 2, 3}
	Clist  = []int32{1, 2, 3}
	// TODO(toddw): test multiple items after ordering is fixed.
	//Cset   = set[int32]{1, 2, 3}
	Cset   = set[int32]{1}
	// TODO(toddw): test multiple items after ordering is fixed.
	//cmap   = map[int32]string{1: "A", 2: "B", 3: "C"}
	Cmap   = map[int32]string{1: "A"}
	Cargs  = Args{1, 2}

	CTVbool      = typeobject(bool)
	CTVstring    = typeobject(string)
	CTVbytes     = typeobject([]byte)
	CTVbyte      = typeobject(byte)
	CTVuint16    = typeobject(uint16)
	CTVint16     = typeobject(int16)
	CTVfloat32   = typeobject(float32)
	CTVcomplex64 = typeobject(complex64)
	CTVenum      = typeobject(NamedEnum)
	CTVArray     = typeobject([3]string)
	CTVList      = typeobject([]string)
	CTVSet       = typeobject(set[string])
	CTVMap       = typeobject(map[string]int64)
	CTVStruct    = typeobject(ScalarsExp)
	CTVUnion     = typeobject(NamedUnion)
	CTVAny       = typeobject(any)
)
